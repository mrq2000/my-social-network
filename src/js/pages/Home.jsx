import React from 'react';

import Layout from '../components/common/Layout';

const Home = () => {
  const content = 'Hello world';
  return (
    <Layout>
      {content}
    </Layout>
  );
};

export default Home;
